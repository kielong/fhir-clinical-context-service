# ORIGIN: AI — drafted by Claude Code, reviewed by Kiel
# data/synthea is ~1.3 GB after extraction; it must never enter the build context.
data/
.git/
.idea/
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.env
*.zip
seed.log
# The Azure deployment files (and the 150 MB database dump in azure/dump/) are not part of the image.
azure/
